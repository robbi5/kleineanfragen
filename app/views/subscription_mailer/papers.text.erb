<%= @first_line %>:

<%- @papers.each do |paper| -%>
* <%= paper.title %>
  <% if paper.major? -%>Große Anfrage • <% end -%>Eingereicht von: <%= paper.originators.collect(&:name).join(', ') %>
  Veröffentlicht am <%= l paper.published_at %> <%- unless paper.page_count.nil? -%>— <%= paper.page_count %> <%= t :pages, count: paper.page_count -%><%- end -%><%- if paper.contains_table %>, enthält Tabelle<% end %>
  <%= paper_short_url(paper) %>

<%- end -%>

-----
Diese Benachrichtigung nicht mehr erhalten:
  <%= unsubscribe_url(@subscription) %>