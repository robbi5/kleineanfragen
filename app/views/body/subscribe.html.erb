<% title "#{@body.name} abonnieren" %>

<div class="page-header">
  <h1><%= link_to @body.name, @body %> per E-Mail abonnieren</h1>
</div>

<div class="row">
  <div class="col-sm-8">
    <%= form_for @subscription, url: subscription_create_path, html: { class: 'form-horizontal' } do |f| %>
      <%= f.hidden_field(:subtype) %>
      <%= f.hidden_field(:query) %>

      <div class="form-group">
        <label class="col-sm-2 control-label" for="email">E-Mail:</label>
        <div class="col-sm-10">
          <%= f.email_field :email, class: 'form-control', placeholder: 'mail@example.com' %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <%= f.submit 'abonnieren', class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-sm-4">
    <p>
      Interessiert daran, schnell von neuen Anfragen in <%= @body.name %> zu erfahren?
      kleineAnfragen verschickt E-Mails an ... 
    </p>
    <p>
      Beim ersten abo an eine mailadresse, doubleoptin.
    </p>
  </div>
</div>